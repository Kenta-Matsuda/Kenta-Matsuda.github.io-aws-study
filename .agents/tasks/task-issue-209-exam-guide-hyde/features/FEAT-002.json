{
  "id": "FEAT-002",
  "type": "feat",
  "description": "Resolve issue #209: make the AI-search HyDE query expansion exam-guide-informed so newer/edge AWS services (the issue's litmus test is 'Quick' → Amazon QuickSight / Amazon Q) surface even when the model was not trained on them. Today expandQueryToAwsKeywords() in js/ui.js relies ENTIRELY on the model to guess concrete service names; the local exam data (per-task/step `knowledge` arrays and `description`/`descriptionEn` service mentions) is never fed back into expansion, so services the model omits are lost. Build a pure, testable exam-guide keyword catalog from the exam data and use it to augment the HyDE expanded-term set with catalog services that are relevant to the user's query. Keep ALL pure logic in js/resourceSearch.js (DOM/network/AI-free) so it is unit-testable offline; the browser/AI-only call stays in js/ui.js. This directly builds on the merged HyDE work (issue #201, PR #204) and reuses searchResourcesMulti + selectAiCandidates unchanged.",
  "status": "completed",
  "steps": [
    "Study the exam data shape before coding: read js/data/clf-c02.js task 3.7 (has 'Amazon QuickSight' in BOTH a `description` bullet and the `knowledge` array), and js/data/aif-c01.js (has `knowledge` AND `knowledgeEn` arrays). Confirm the traversal you need mirrors buildResourceIndex in js/resourceSearch.js: exam.steps[] (step.knowledge, step.description/descriptionEn) AND exam.domains[].tasks[] (task.knowledge/knowledgeEn, task.description/descriptionEn).",
    "Add a pure function to js/resourceSearch.js: `buildExamKeywordCatalog(exams = ALL_EXAMS)`. It walks the same step + domain/task structure buildResourceIndex uses and collects a de-duplicated, order-stable list of important AWS service names / concept keywords from each task/step's `knowledge` and `knowledgeEn` arrays (bare strings). Accept string or array; trim; drop empties. Return an array of unique keyword strings (case-insensitive dedupe, preserving first-seen original casing). Keep it DOM/network-free. Add a JSDoc header in the same style as the existing functions explaining WHY (issue #209: the exam guide is the durable source of 'which services/concepts matter', including newer ones like QuickSight that the LLM may not know) and that it must stay consistent with buildResourceIndex's traversal.",
    "Add a second pure function to js/resourceSearch.js: `augmentTermsWithCatalog(terms, catalog, opts)` that, given the HyDE expanded `terms` (array of strings) and a `catalog` (from buildExamKeywordCatalog), returns terms UNION the catalog entries that are relevant to the query terms — where a catalog entry is 'relevant' if any provided query term (case-insensitive) is a substring of the catalog entry OR the catalog entry is a substring of a query term (so a partial term like 'Quick' pulls in 'Amazon QuickSight', and 'QuickSight' pulls in the catalog's 'Amazon QuickSight'). De-dupe case-insensitively, preserve order (original terms first, then newly-added catalog terms in catalog order), and cap the result with an `opts.limit` (default e.g. 24) to bound prompt/candidate size. Never mutate the inputs. JSDoc it. Rationale: this is the 'organize which services/concepts are important per exam guide, then use that to inform HyDE' the issue asks for, kept as pure logic.",
    "Wire it into js/ui.js. In getResourceSearchIndex-adjacent code add a cached `getExamKeywordCatalog()` (mirroring the existing lazy `resourceSearchIndex` cache at ~line 4917) that calls buildExamKeywordCatalog() once. Import buildExamKeywordCatalog and augmentTermsWithCatalog from './resourceSearch.js' (extend the existing import at line 80). In the AI-search handler (wireResourceSearchHandlers, ~line 5172) where it currently does `const terms = [query, ...expandedTerms];`, change it to also fold in catalog-relevant terms: e.g. `const baseTerms = [query, ...expandedTerms]; const terms = augmentTermsWithCatalog(baseTerms, getExamKeywordCatalog());`. This means even if the model omits 'Amazon QuickSight', a query containing 'Quick'/'QuickSight' still pulls the catalog entry so searchResourcesMulti finds the published QuickSight resources. Do NOT change searchResourcesMulti or selectAiCandidates.",
    "Consider also seeding the model prompt: OPTIONAL and only if low-risk — you MAY pass a short, query-relevant slice of the catalog into expandQueryToAwsKeywords's system/user prompt as 'known important services for these exams' so the model is grounded on newer services. If you do, keep it bounded (reuse augmentTermsWithCatalog against the raw query to pick which catalog entries to mention, cap ~12) and keep the parse/return contract of parseExpandedKeywords unchanged. If it adds risk or complexity, skip it and rely on the deterministic post-expansion augmentation from the previous step (that alone satisfies the issue's litmus test). Document which choice you made.",
    "Add tests/exam-guide-hyde.spec.mjs following the pure-logic style of tests/ai-search-hyde.spec.mjs (import from '@playwright/test', use a makeExam()/synthetic helper for determinism; do NOT depend on real data counts except in one smoke check). Cover buildExamKeywordCatalog: pulls knowledge + knowledgeEn from both step-level and task-level; trims and drops empties; de-dupes case-insensitively preserving first casing; accepts string or array. Cover augmentTermsWithCatalog: a partial query term 'Quick' pulls in catalog 'Amazon QuickSight'; 'QuickSight' also pulls it; already-present terms are not duplicated; order is preserved (original terms first); respects the limit; does not mutate inputs; empty/whitespace query terms are ignored. Add ONE smoke test against the real data: `buildExamKeywordCatalog()` (default ALL_EXAMS) contains an entry whose lowercase includes 'quicksight' (proving the #209 litmus service is in the catalog), and that augmentTermsWithCatalog(['Quick'], catalog) yields a term whose lowercase includes 'quicksight'.",
    "Add a chained end-to-end pure assertion in the same spec (no browser): build the real resource index and catalog, simulate the model returning NO QuickSight term (e.g. expandedTerms = ['Amazon Athena']), then terms = augmentTermsWithCatalog(['Quick', ...expandedTerms], catalog); assert searchResourcesMulti(index, terms) returns at least one record whose title/titleEn/url contains 'QuickSight' (case-insensitive). This proves the fix makes 'Quick' surface QuickSight resources even when the model omits it — the exact scenario in issue #209.",
    "Run static verification: `env -u NODE_OPTIONS node --check` on js/resourceSearch.js, js/ui.js, and tests/exam-guide-hyde.spec.mjs. Run the new spec's assertions with plain node by importing the pure functions (Playwright cannot run offline) — either extract the pure-function assertions into a throwaway `env -u NODE_OPTIONS node --input-type=module -e '...'` run, or a small temporary .mjs you delete after. Confirm all assertions pass. Record exact commands + outputs.",
    "Write docs/issues/exam-guide-hyde-209.md mirroring the structure of docs/issues/ai-search-hyde-201.md (背景 / 実装方針 / 変更ファイル / 考慮したトレードオフ / 検証 + 手動再現手順). Explain the 'Quick' litmus test, the exam-guide catalog approach, and that the AI call is browser-only (verified manually) while the catalog + augmentation is pure and unit-tested. Add a link to it in docs/index.md under the issues section (match the existing entry format; no dead links).",
    "Commit on feature/issue-209-exam-guide-hyde with a Japanese conventional-commit message, e.g. `feat: AI検索のHyDEクエリ拡張を試験ガイド由来のサービス辞書で補強し、QuickSight等の新しいサービスを検索に出す (#209)`. Stage only the specific files changed (js/resourceSearch.js, js/ui.js, tests/exam-guide-hyde.spec.mjs, docs/issues/exam-guide-hyde-209.md, docs/index.md) — do NOT use `git add -A`. Do NOT push. Do NOT open a PR."
  ],
  "acceptance_criteria": [
    "js/resourceSearch.js exports two new pure functions: buildExamKeywordCatalog(exams=ALL_EXAMS) and augmentTermsWithCatalog(terms, catalog, opts). Both are DOM/network/AI-free.",
    "buildExamKeywordCatalog collects knowledge + knowledgeEn from step-level and task-level, trims, drops empties, de-dupes case-insensitively preserving first casing; the default (real ALL_EXAMS) catalog contains an entry whose lowercase includes 'quicksight'.",
    "augmentTermsWithCatalog(['Quick'], catalog) returns a term whose lowercase includes 'quicksight'; existing terms are not duplicated; order is original-terms-first; the limit is respected; inputs are not mutated.",
    "js/ui.js AI-search handler folds catalog-relevant terms into the HyDE term set via augmentTermsWithCatalog + a cached getExamKeywordCatalog(); searchResourcesMulti and selectAiCandidates are unchanged.",
    "A pure end-to-end assertion proves that with the model omitting QuickSight, a query of 'Quick' still yields at least one real QuickSight resource via searchResourcesMulti(index, augmentedTerms).",
    "tests/exam-guide-hyde.spec.mjs exists in the pure-logic style and covers the catalog + augmentation behavior plus the real-data smoke/e2e assertions.",
    "docs/issues/exam-guide-hyde-209.md exists and is linked from docs/index.md.",
    "`env -u NODE_OPTIONS node --check` passes for js/resourceSearch.js, js/ui.js, tests/exam-guide-hyde.spec.mjs.",
    "Changes are committed on feature/issue-209-exam-guide-hyde and NOT pushed."
  ],
  "verification": [
    "env -u NODE_OPTIONS node --check js/resourceSearch.js && env -u NODE_OPTIONS node --check js/ui.js && env -u NODE_OPTIONS node --check tests/exam-guide-hyde.spec.mjs",
    "env -u NODE_OPTIONS node --input-type=module -e \"import('./js/resourceSearch.js').then(m=>{const cat=m.buildExamKeywordCatalog();const hasQS=cat.some(k=>k.toLowerCase().includes('quicksight'));const aug=m.augmentTermsWithCatalog(['Quick'],cat);const augQS=aug.some(k=>k.toLowerCase().includes('quicksight'));const idx=m.buildResourceIndex();const terms=m.augmentTermsWithCatalog(['Quick','Amazon Athena'],cat);const res=m.searchResourcesMulti(idx,terms);const qsRes=res.some(r=>((r.title||'')+(r.titleEn||'')+(r.url||'')).toLowerCase().includes('quicksight'));if(!(hasQS&&augQS&&qsRes)){console.error({hasQS,augQS,qsRes});process.exit(1);}console.log('ALL_209_ASSERTIONS_PASS');})\"",
    "env -u NODE_OPTIONS node -e \"['js/locales/ja.json','js/locales/en.json'].forEach(f=>JSON.parse(require('fs').readFileSync(f,'utf8')));console.log('json ok')\"  # only if any locale JSON was touched",
    "git -C . log --oneline -1  # confirm the #209 commit is on feature/issue-209-exam-guide-hyde",
    "git status --short  # expect clean working tree after commit (no stray temp files)"
  ],
  "blocked_reason": null,
  "findings": "COMPLETED on feature/issue-209-exam-guide-hyde. Commit 32f819f: `feat: AI検索のHyDEクエリ拡張を試験ガイド由来のサービス辞書で補強し、QuickSight等の新しいサービスを検索に出す (#209)` (5 files changed, 457 insertions, 2 deletions). NOT pushed (orchestrator pushes).\n\nDATA SHAPE NOTE: grep_search gave FALSE NEGATIVES for 'knowledge'/'knowledgeEn' in js/data/*.js, but a node walk of ALL_EXAMS proved both fields exist: stepKnow=16, taskKnow=206, stepKnowEn=16, taskKnowEn=189 across 13 exams. buildExamKeywordCatalog therefore collects knowledge+knowledgeEn at BOTH step level (exam.steps[]) and task level (exam.domains[].tasks[]), mirroring buildResourceIndex's traversal. Verified via: `env -u NODE_OPTIONS node --input-type=module -e \"import('./js/exams.js')...\"`.\n\nIMPLEMENTATION:\n- js/resourceSearch.js: added 2 pure (DOM/network/AI-free) exported functions — buildExamKeywordCatalog(exams=ALL_EXAMS) [trim, drop empties, accept string|array, case-insensitive dedupe preserving first casing] and augmentTermsWithCatalog(terms, catalog, {limit=24}) [union of terms + query-relevant catalog entries via bidirectional substring match, order = original terms then catalog order, case-insensitive dedupe, capped by limit, never mutates inputs]. searchResourcesMulti/selectAiCandidates UNCHANGED.\n- js/ui.js: extended the resourceSearch.js import (line ~80); added memoized examKeywordCatalog + getExamKeywordCatalog() next to getResourceSearchIndex(); in wireResourceSearchHandlers changed `const terms = [query, ...expandedTerms]` to `const baseTerms = [query, ...expandedTerms]; const terms = augmentTermsWithCatalog(baseTerms, getExamKeywordCatalog());`.\n- OPTIONAL step 5 (seeding the model prompt with a catalog slice): SKIPPED by design to keep risk low — the deterministic post-expansion augmentation alone satisfies the #209 litmus test. Documented in docs/issues/exam-guide-hyde-209.md section 4.\n- tests/exam-guide-hyde.spec.mjs: pure-logic spec (import from '@playwright/test', makeExam() synthetic helper) covering catalog (both levels, trim/empties, case-insensitive dedupe w/ first casing, string|array, empty exams) + augment (Quick pulls QuickSight, exact QuickSight pulls it, catalog-substring-of-query, no dup, order, limit, empty terms ignored, no mutation) + real-data smoke + chained e2e (model omits QuickSight -> 'Quick' still surfaces a real QuickSight resource via searchResourcesMulti).\n- docs/issues/exam-guide-hyde-209.md (mirrors ai-search-hyde-201.md structure: 背景/実装方針/変更ファイル/考慮したトレードオフ/検証+手動再現手順) and linked from docs/index.md right after the #201 entry.\n\nVERIFICATION (all under `env -u NODE_OPTIONS`, node v22; Playwright cannot run offline under INTEGRATIONS_ONLY as expected):\n1) `node --check js/resourceSearch.js && node --check js/ui.js && node --check tests/exam-guide-hyde.spec.mjs` => CHECK_OK (all pass).\n2) Ran the FEAT-002 assertion one-liner (import resourceSearch.js; build real catalog+index): hasQS && augQS && qsRes all true => printed ALL_209_ASSERTIONS_PASS.\n3) Ran every pure assertion from the new spec via a throwaway node --input-type=module -e run => printed SPEC_ASSERTIONS_ALL_PASS (no temp files left; git status clean apart from this task-state file).\n4) `git log --oneline -1` confirms the #209 commit is HEAD of feature/issue-209-exam-guide-hyde. `git status --short` clean (only this FEAT-002.json tracking file modified, intentionally not committed with the feature).\nNo JSON/locale files were touched, so the JSON-parse step was N/A. No `aws` commands run. Not pushed; no PR opened.\n\n=== v1 review follow-up (commit 636c7ae on feature/issue-209-exam-guide-hyde) ===\nThe v1 semantic review (2026-09-12-182054-review.md) returned NEEDS_CHANGES with 5 issues. All addressed on the SAME branch (status stays 'completed', no block). Commit 636c7ae: `fix: #209 レビュー指摘対応 …` (4 files changed, 344 insertions, 27 deletions). Files: js/resourceSearch.js, js/ui.js, tests/exam-guide-hyde.spec.mjs, docs/issues/exam-guide-hyde-209.md. NOT pushed.\n\nIssue 1 (litmus not moved): Confirmed empirically that bare 'Quick' is inert (raw substring search already finds QuickSight: raw=6, augmented=6). The real win is verbose/multi-token queries and no-overlap concept queries. Preserved the verbose benefit ('Amazon Q Developer とは' 0->9) and ADDED recall for no-lexical-overlap concept queries via a curated alias (issue 5). Documented honestly in docs that bare 'Quick' is already-searchable (no-op, not a regression) and the real gap is verbose + concept queries.\n\nIssue 2 (non-reversion-sensitive test): Replaced the old ['Quick','Amazon Athena'] test with two DIFFERENTIAL tests that compare augmented vs un-augmented (`[query,...expandedTerms]`) result sets on the same real index: (a) verbose 'Amazon Q Developer とは' asserts augmentedResults.length > baseline (baseline=0) and delta non-empty; (b) 'BIツール' asserts QuickSight surfaces only with augmentation. PROOF of reversion-sensitivity (throwaway node run replacing augmentTermsWithCatalog with identity): `[verbose] baseline=0 augmented(reverted)=0 assertion(augmented>baseline) => false -> correctly FAILS`; `[BIツール] augmented(reverted) hasQS => false -> correctly FAILS`. => 'Reversion caused 2 of 2 differential assertions to FAIL (expected: 2).' With augmentation present, full spec = `SPEC RESULT: 26 passed, 0 failed, 26 total`.\n\nIssue 3 ('Amazon Q' over-matches): Added pure exported `isSafeCatalogSearchTerm(entry)` — rejects entries whose punctuation-stripped token list has a <2-char token AND <=2 total tokens (no anchor). Empirically filters exactly the 'Amazon Q' family (2 of 1294 catalog entries) while KEEPING 'Amazon Q Developer'/'Amazon Q Business'. augmentTermsWithCatalog now skips unsafe entries before adding them as live search terms. Verified: `searchResources(idx,'Amazon Q')`=38 noisy hits (queue/quotas/parquet) no longer injected; verbose query now recovers via anchored 'Amazon Q Developer' (0->9). Unit-tested (4 guard tests).\n\nIssue 4 (grounding scored on raw query): Added pure exported `buildAugmentedScoringQuery(terms)` (dedup+join). ui.js now calls `selectAiCandidates(candidates, buildAugmentedScoringQuery(augmentedTerms), 40)` instead of the raw query, so catalog-only hits score >0 and reach the model. selectAiCandidates/scoreResourceRelevance/searchResourcesMulti UNCHANGED. Unit-tested (3 tests incl. a catalog-only-hit scoring test).\n\nIssue 5 (vague-query half): Added small curated pure `CONCEPT_SERVICE_ALIASES` (BIツール/ダッシュボード/business intelligence/dashboard/ビジネスインテリジェンス -> Amazon QuickSight). augmentTermsWithCatalog folds aliased services (only those present in catalog) into the query-term set so no-overlap concept queries pull the service. Verified 'BIツール' 0->2 with QuickSight. LIMITATION explicitly documented in docs/issues/exam-guide-hyde-209.md: this is NOT an exhaustive concept map; general concept->service association (e.g. embedding search) is out of scope. No heavy machinery / external deps added.\n\nVERIFICATION (env -u NODE_OPTIONS, node v22): `node --check` on resourceSearch.js, ui.js, spec => CHECK_OK. FEAT-002 assertion one-liner => ALL_209_ASSERTIONS_PASS (still holds). Full spec via a throwaway @playwright/test shim+loader (deleted after) => 26 passed / 0 failed. Reversion proof above. No locale/JSON touched. docs/index.md link to exam-guide-hyde-209.md still valid; no GitHub task-list checkbox syntax in docs. Only feature files staged (git add by name; task.json + review .md intentionally NOT committed). No aws commands. Not pushed; no PR opened.\n\n=== v2 review follow-up (commit cd94e31 on feature/issue-209-exam-guide-hyde) ===\nThe v2 semantic review APPROVED the branch but flagged ONE non-blocking precision wart: the CONCEPT_SERVICE_ALIASES matching loop in augmentTermsWithCatalog used a bidirectional test `q.includes(conceptLower) || conceptLower.includes(q)`. The reverse direction over-fired — a short/generic query token that is merely a substring of a concept word injected the aliased service (e.g. 'b', 'business', 'ビジネス' all pulled 'Amazon QuickSight' because they are substrings of concept keys 'business intelligence'/'ビジネスインテリジェンス'). TIGHTENED to the forward direction ONLY (`q.includes(conceptLower)`): the alias fires only when the QUERY CONTAINS the concept phrase. Exact concept queries still fire because q===conceptLower makes q.includes(conceptLower) true. The separate catalog-entry relevance loop below it (still bidirectional `entryLower.includes(q) || q.includes(entryLower)`) was intentionally left UNCHANGED so a partial term like 'Quick' still pulls 'Amazon QuickSight'.\nFiles: js/resourceSearch.js (1-line hit-test change + comment), tests/exam-guide-hyde.spec.mjs (added 2 guard tests: a query CONTAINING 'ダッシュボード' still pulls QuickSight; generic substrings 'business'/'b'/'ビジネス' do NOT), docs/issues/exam-guide-hyde-209.md (updated the 指摘 5 sentence to state the forward-direction semantics). Commit cd94e31: `fix: #209 概念エイリアスの過剰発火を修正（クエリが概念語を含む場合のみ発火）` (3 files, 28 insertions, 7 deletions). NOT pushed.\nVERIFICATION (env -u NODE_OPTIONS, node v22): `node --check` on js/resourceSearch.js + tests/exam-guide-hyde.spec.mjs => SYNTAX_OK. Pure-logic proof: augmentTermsWithCatalog(['BIツール'],['Amazon QuickSight'])=>includes QuickSight (true); ['ダッシュボードを作りたい']=>true; ['business']/['b']/['ビジネス']=>NOT included (false). Real-data (buildExamKeywordCatalog+buildResourceIndex+searchResourcesMulti): 'BIツール' => {injected:true, found:true}; 'business' => {injected:false, found:false}. Full spec via a throwaway @playwright/test shim (deleted after) => 28 passed, 0 failed. Differential/reversion proof: reverting the fix (restoring the reverse direction) made the new 'no reverse fire' guard FAIL (27 passed, 1 failed), confirming the guard is reversion-sensitive. Only the 3 named files staged (git add by name). No aws commands. Not pushed; no PR opened (orchestrator handles)."
}
