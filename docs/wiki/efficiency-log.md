# 効率化・自己拡張ログ

- 最終更新日: 2026-09-06
- 対象範囲: 作業の効率化・低コスト化（繰り返し作業のスクリプト化によるトークン削減）の検討ログと、自己拡張（プロンプト・skill の改善）提案ログ
- 出典/参照: issue #69 / `.kiro/agents/exam-content-maintainer.md` / `.kiro/agents/github-issue-resolver.md`

> エージェントが実行のたびに自分の作業を振り返り、**トークン消費と手間を減らす**アイデアと、**自分自身の能力を拡張する**提案を記録するログです。スキーマ（メタデータ + 更新履歴）は [README](README.md) を参照してください。

## 記録の目的

- 「LLM が毎回自然言語で処理していた定型作業」を「スクリプト + 結果の要約読み込み」に置き換え、**コンテキスト投入トークンを削減**する。
- 効率化・自己拡張の**検討結果と効果（見込み / 実測）**を残し、次回以降の自分が参照できるようにする。
- 自己改変（プロンプト / skill / スクリプト）は必ず **PR ベース・人間レビュー前提**で提案する。

## 効率化アイデアのエントリ形式（例）

各エントリは次の項目で記録します。以下は**記入例（サンプル）**であり、実測値ではありません。

### 例: リンク死活チェックのスクリプト化

- **日付**: 2026-09-03（例）
- **対象作業**: `js/data/*.js` の全 `url` / `urlEn` を LLM が手作業で抽出し 1 件ずつ確認していた。
- **課題**: URL 抽出と確認を自然言語で毎回行うとトークン消費が大きく、再現性も低い。
- **アイデア**: `scripts/` に「全試験ファイルから `url`/`urlEn` を抽出し HTTP ステータスを一覧化する」リンクチェッカを実装し、LLM は**結果サマリだけ**を読み込む。
- **想定効果（見込み）**: URL 一覧の生投入が不要になり、確認 1 巡あたりの投入トークンを削減。
- **状態**: アイデア（未実装）。実装時は `env -u NODE_OPTIONS node --check` で静的検証し、PR で提案する。

## 自己拡張（プロンプト・能力）提案のエントリ形式（例）

- **日付**: YYYY-MM-DD
- **対象**: `.kiro/agents/exam-content-maintainer.md`（本エージェントのプロンプト） / `.kiro/` 配下の skill など。
- **提案内容**: 追記・改善する手順 / 判断基準 / 禁止事項の具体。
- **理由・期待効果**: なぜ必要か、何が改善されるか。
- **想定リスク**: 権限緩和の有無など（権限緩和は特に慎重に、理由を明記して PR レビューを仰ぐ）。
- **状態**: 提案 / 実装済み（PR 番号）など。

## 自己拡張提案ログ（実績）

### 2026-09-05: コメントファースト前提条件の明文化とレトロスペクティブ/自己改善ループの制度化

- **日付**: 2026-09-05
- **対象**: `.kiro/agents/github-issue-resolver.md`（issue-resolver エージェントのプロンプト） / 本ログ `docs/wiki/efficiency-log.md`。
- **提案内容**:
  1. コメントファーストのハード前提条件を `手順` の先頭（新・手順 1）へ移設。どの issue でも、着手・見送り（スキップ）判断の前に、issue 自身のコメントと、関連するすべての PR（open / closed / merged）のコメント 3 種（ディスカッション `/issues/{n}/comments`、インライン `/pulls/{n}/comments`、レビュー `/pulls/{n}/reviews`）を必ず読むことを無条件で要求。「オープン PR がある」ことは、それ単独ではスキップ理由にならないと明記。
  2. 「振り返り（レトロスペクティブ）と自己改善ループ（必須）」節を新設。バッチ着手時に「なぜこれまで対応されずに残っていたのか」を自省して根本原因を特定し、プロセス改善を恒久ルール（本マニュアル / `docs/`）へ書き戻して独立した `chore:` 仕組み変更 PR として発行することを義務化。振り返りは本ログに日付付きで記録し、最終報告にも要約と仕組み変更 PR を含めることを必須化。
- **理由・期待効果**: コメントファーストの指針は既に「既存オープン PR の新規コメント対応」節に存在したが、埋もれていて参照されず遵守されなかった。その結果、オープン PR（PR #114 / issue #32、PR #113 / issue #109）に付いていたユーザーのエスカレーション済みコメントが未対応のまま放置された。前提条件を先頭へ移し、かつ自省→仕組み反映のループを制度化することで、同種の見落としを恒久的に防ぎ、改善が積み重なって加速する。
- **想定リスク**: 権限緩和・既存制約の削除は無し（既存の禁止事項はすべて維持し、制約の追加・明確化・再配置による強化のみ）。
- **状態**: 実装済み（本 PR）。

### 2026-09-06: 静的サイトからのリモート MCP 利用検討義務と「実現不能」断定回避、PR 乱立抑制の恒久化

- **日付**: 2026-09-06
- **対象**: `.kiro/agents/github-issue-resolver.md`（issue-resolver エージェントのプロンプト） / 本ログ `docs/wiki/efficiency-log.md`。
- **提案内容**: PR #139（AWS Knowledge MCP 関連）および issue #138 の対応で顕在化した 3 つの根本原因に対し、恒久的な運用ルールを本マニュアルへ追加した。
  1. **根本原因(a): PR の乱立と再レビュー待ちの滞留。** 多くの `[Feedback]` 系 issue が個別 PR を次々生み、いずれもメンテナの再レビュー待ちで滞留した。→ 対策: 「PR 乱立・再レビュー待ちの滞留を防ぐ（既存 PR 優先の再徹底）」を新設し、同一 issue には新規 PR より既存オープン PR ブランチの更新を優先すること、関連する `[Feedback]` issue が重複する場合は関連オープン PR 番号を把握・明記して重複 PR を作らないことを、既存のコメントファースト・ゲートの強化として明文化。
  2. **根本原因(b): 「静的サイトは MCP を使えない / バックエンド必須」という技術的誤解。** 設計ドキュメント横断で繰り返されていた。実際にはブラウザからリモートの LLM / MCP HTTP エンドポイントを直接呼べる（`js/gemini.js` が既に `https://generativelanguage.googleapis.com` をブラウザ `fetch()` で直接呼んでいる）。真のブロッカーは CORS / 認証 / レート制限であってバックエンドの有無ではない。→ 対策: 「『静的サイトだからバックエンド必須』という短絡的な断定をしない」を新設し、実現不能 / バックエンド必須と記録する前にブラウザからの直接呼び出し可否を評価し、残存ブロッカーを CORS / 認証 / レート制限として具体化することを義務化。
  3. **根本原因(c): ネットワーク制限下での過度に確信的な「実現不能」断定。** INTEGRATIONS_ONLY 環境で外部ドキュメント / エンドポイントを検証できないことが、過信した「不可能」結論につながった。→ 対策: 「ネットワーク制限下での『実現不能』断定の禁止」を新設し、検証できない外部事実は断定せず `要確認 / 公式ドキュメントで要確認` と留保として記録し、クライアントサイドで前進できる部分は切り離して出荷することを明文化。
- **理由・期待効果**: 3 つの根本原因を仕組みへ書き戻すことで、同種の見落とし（重複 PR の量産、誤った実現不能判断、外部未検証を理由にした過度な断定）を恒久的に防ぎ、クライアントサイドで前進できる範囲を最大化する。
- **想定リスク**: 権限緩和・既存制約の削除は無し（AWS 操作禁止・`main` 直接 push 禁止・force push 禁止・秘密情報の非コミット・`gh api` 利用・PR 本文のタスクリスト記法禁止などの既存の禁止事項はすべて維持し、制約の追加・明確化・強化のみ）。
- **状態**: 実装済み（本 PR）。

### 2026-09-06: 「可能／不可能」の断定はコードを読む前に実測する（CORS 検証の型を確立）

- **日付**: 2026-09-06
- **対象**: `docs/wiki/aws-resource-discovery.md`（探索ノウハウ） / 本ログ / `docs/action-required/issue-138-ai-chat-exam-grounding.md`。
- **背景（同日の直前エントリの反省）**: 同日の前エントリで「静的サイトだからバックエンド必須という短絡を避ける」ルールを追加したが、その適用結果として今度は逆方向に振れ、「リモート／ホスト型 MCP ならクライアントから直接呼べる**可能性がある**」と推論だけで書き換えてしまった。ネットワーク可能な環境で実測したところ、**AWS の両 MCP エンドポイントは CORS の制約で実際にブラウザから呼べない**ことが判明した。つまり 1 日のうちに「不可能」と「可能そう」の両方を、どちらも実測せずに書いていた。
- **根本原因**: 外部エンドポイントの可否を**推論で決めていた**こと。「静的サイトだから無理」も「リモート HTTP なら行ける」も、どちらも 1 回の `curl` で決着する問いだった。
- **提案内容（恒久ルール）**:
  1. 外部エンドポイントの利用可否を文書に書くときは、**ネットワーク可能な環境で実際にリクエストを投げ、レスポンスヘッダーを添えて記録する**。ブラウザ実行可否を判断する場合は、`Origin` ヘッダー付きの実リクエストと `OPTIONS`（プリフライト）の両方を確認し、`Access-Control-Allow-Origin` / `Access-Control-Expose-Headers` / プリフライトのステータスを根拠として残す。
  2. 実測できない環境では、`不可能` とも `可能` とも書かず `要確認` に留める（前エントリのルールを維持）。ただし**実測できる環境に移ったら最優先で実測して結論を確定させる**。
  3. 再現コマンドを設計ドキュメントに残す。AWS 側の設定はいずれ変わるため、次回は同じコマンドを流すだけで再判定できる。
- **効率化の効果（実測）**: 今回、`curl` 3 本（`initialize` / `tools/call` / `OPTIONS`）で「MCP は使えるのか」という論点が確定した。推論と文書修正の往復（PR #139 で 2 回の前提書き換えが発生）を、次回からは数十トークンのコマンド実行に置き換えられる。再現コマンドは `docs/action-required/issue-138-ai-chat-exam-grounding.md` に記載済み。
- **副産物（同じ型で見つかった実バグ）**: リンク死活も同じ「推論せず実測」で確認したところ、`js/data/` の公式試験ガイド PDF 4 URL（MLA-C01 / AIP-C01 の日英）が 403 だった。`getExamOfficialRefs()` を回して全 52 URL を一括 `fetch` する使い捨てスクリプトで検出しており、この手順は `docs/wiki/aws-resource-discovery.md` に手順として残した（効率化アイデア「リンク死活チェックのスクリプト化」の部分実践）。
- **想定リスク**: 無し（検証は読み取り専用の HTTP リクエストのみ。AWS リソースへの操作は行っていない）。
- **状態**: 実装済み（本 PR）。

## 更新履歴

- 2026-09-03: 初版作成（issue #69）。効率化アイデアと自己拡張提案の記録形式・記入例を整備。
- 2026-09-05: コメントファースト前提条件の明文化とレトロスペクティブ/自己改善ループ制度化の自己拡張提案（実装済み）を追記。根本原因はコメントファーストのルールが埋もれて参照されず、オープン PR #114（issue #32）/ #113（issue #109）のエスカレーション済みコメントが未対応で残っていたこと。
- 2026-09-06: 静的サイトからのリモート MCP 利用検討義務、ネットワーク制限下での「実現不能」断定回避（外部事実は要確認として留保）、PR 乱立・再レビュー待ち滞留の抑制（既存 PR 優先の再徹底）を運用ルールへ恒久反映（実装済み）。根本原因は PR #139 / issue #138 対応で顕在化した 3 点（PR 乱立と再レビュー待ち滞留、「静的サイト=バックエンド必須」という技術的誤解、ネットワーク制限下での過信した不可能判断）。権限緩和・既存制約の削除は無し。
- 2026-09-06: 上記ルールの適用が逆方向に振れた反省を追記。「リモートなら呼べる可能性がある」も推論であり、`Origin` 付き実リクエストと `OPTIONS` プリフライトの実測（+ 再現コマンドの記録）で結論を確定させることを恒久ルール化。実測により AWS の両 MCP エンドポイントは CORS で呼べないことが確定し、代替として Gemini `url_context` を採用。同じ「推論せず実測」で公式試験ガイド PDF 4 URL の 403 も検出し、HTML 版へ差し替え。
